{control commentNewForm}
{foreach $newestComments as $comment}
	<div class="comment">
		{$comment->comment}
	</div>
{/foreach}
{if count($allComments) > $commentsNumber}
	<div class="full-comments">
		{foreach $allComments as $comment}
			{if $iterator->counter > $commentsNumber}
				<div class="comment">
					{$comment->comment}
				</div>
			{/if}
		{/foreach}
	</div>
{/if}
<button class="show-all">Ukázat všechny komentáře ({count($allComments) - $commentsNumber})</button>