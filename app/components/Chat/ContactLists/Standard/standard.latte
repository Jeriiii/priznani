
<ul id="contacts">
	{foreach $contacts as $contact}
		<li id="contact-{$contact->id}" data-id="{$contact->id}" data-title="{$contact->user_name}" class="contact-link">{$contact->user_name}</li>
	{/foreach}
</ul>

<script>
	var mydata = {
		name: {$username}
	}
</script>
