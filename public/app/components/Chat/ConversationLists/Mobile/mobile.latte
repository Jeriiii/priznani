<div id="conversations">
	<ul n:snippet="conversations" data-ajax-append="true">
			{foreach $conversations as $conversation}
				{var $id = $userfinder->getCorrectCodedId($conversation->id_sender, $conversation->id_recipient)}
				{var $user = $userfinder->getCorrectUser($conversation->id_sender, $conversation->id_recipient)}
				{var $unreaded = $conversation->readed == 0 && $userId != $conversation->id_sender}
				<li class="{if $unreaded}unreaded{/if}">
					<a href="{plink ':Chat:default', 'userInChatID' => $user->id}" class="conversation-link"></a>{* volán javascriptem *}
					{$user|showProfMinDiv}
						<strong>{$user->user_name}</strong>
						<p class="lastmessage">{$conversation->text|truncate:35}</p>
					<div class="clear"></div>
				</li>
			{/foreach}
	</ul>
	<div href="#" class="stream-btn-next">
		<a id="next-data-item-btn" n:href="load!" class="ui-btn">zobrazit více...</a>
	</div>
	<div class="conversations-info-message"></div>
	<div id="stream-loader"><img src="{$basePath}/images/loadingGifs/skypelike.GIF"></div>	
</div>

